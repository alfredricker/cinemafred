/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: []
  },

  typescript: {
    ignoreBuildErrors: false
  },

  reactStrictMode: true
}

module.exports = nextConfig